# Real production data pulled from Grafana/DCGM/LiteLLM — DO NOT publish.
# Contains real hostnames, internal IPs, GPU UUIDs, agency team names,
# and confidential model names. Kept local only.
RAW_DATA/

# Python venv (local ETL tooling)
.venv/

# Tooling / OS junk
.playwright-mcp/
.DS_Store
__pycache__/
*.pyc

# Working/debug screenshots — not part of the deliverable
*.png

# Real (confidential) dashboard dataset — local-only, never commit
data_real/

# Confluence documentation source — names confidential models and real internals.
# Was untracked but NOT ignored, so a `git add .` would have committed it.
# If this ever needs to be shared, scrub the confidential model names first.
docs/

# Real agency usage-attribution data (e.g. per-HTD token breakdowns) — same
# sensitivity class as data_real/. Was untracked but NOT ignored.
teammate.md
